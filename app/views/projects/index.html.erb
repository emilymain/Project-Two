<h1>Projects</h1>
<ul>
  <%@projects.each do |project|%>
    <li>
      <%= project.title %>
      by
      <%= User.find(project.creator).name %>
    </li>
    <li>
      <%= link_to 'project', project_path(project) %>
    </li>
  <%end%>
</ul>
